<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /hersil_php/public/
    
    # No reescribir archivos y directorios existentes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Reescribir todo lo demás a index.php
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>
```

---

**Por ahora, prueba acceder directamente con:**
```
http://localhost/hersil_php/public/views/auth/login.php